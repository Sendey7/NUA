<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.kharkov.nua"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="4"
        android:targetSdkVersion="10" android:maxSdkVersion="21"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".NewsActivity"
            android:label="@string/title_activity_main_activity_nua2" >
        </activity>
        <activity android:name=".NewsBrowser"></activity>
        <activity android:name="startActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="TeachersActivity"></activity>
        <activity android:name="TeachersBrowser"></activity>
        <activity android:name="DiaryActivity"></activity>
        <activity android:name="DiaryLogin"></activity>
    </application>

</manifest>